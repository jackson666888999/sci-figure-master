### 欢迎关注R语言数据分析指南

>本节来复现绘制**NC**上的一张图，通过**ggplot2与ggsankey**绘制全球植物病原真菌分布图，通过桑基图与地图的组合形式来进行数据展示，整个过程仅参考。希望对各位观众老爷能有所帮助。**数据代码已经整合上传到2023VIP交流群**，加群的观众老爷可自行下载，有需要的朋友可关注文末介绍加入VIP交流群。

### 关于永久群内容的说明
>给予长期支持我们的忠实读者们一个特别待遇：凡是购买过小编2022年或2023年VIP会员文档的朋友们，**将自动获得2024年及以后更新的绘图文档代码，无需额外付费。**目前这两年的会员文档已累记卖出1500+，质量方面各位无需担忧。简要概括就是只要购买任意1年的会员内容，2024及后期公众号所更新的绘图文档均会在已经加入的会员群内分享。

### 论文原文

![](https://files.mdnice.com/user/7338/40147538-2a72-4252-99ae-c76f4d78d9e4.png)

![](https://files.mdnice.com/user/7338/f5c1d29e-2007-4403-b041-c60a81267fb2.png)


### 加载R包
```r
library(tidyverse)
library(readxl)
library(sf)
library(ggspatial)
install.packages("devtools")
devtools::install_github("davidsjoberg/ggsankey")
library(ggsankey)
library(patchwork)
```
![](https://files.mdnice.com/user/7338/55879dbf-6607-4f0b-94a0-f9d2f0ff7864.png)

### 导入数据
```r
df <- read_excel("41467_2023_42142_MOESM8_ESM.xlsx") %>% 
  select(1,2,3,4,5,6,7)
```
### 地图绘图
```r
p1 <- df %>% select(4,5,6) %>% 
  # 添加一个名为Global的新列，所有行的值都是"Global"
  mutate(Global="Global") %>% 
  # 将数据转换为长格式，以便于绘制桑基图
  make_long(.,Global,Continent,LandCoverType,Habitat) %>%
  # 使用ggplot开始绘图，设置桑基图的各种参数
  ggplot(.,aes(x = x, next_x = next_x, node = node,
               next_node = next_node,
               label = node)) +
  # 添加桑基流图层，设置流的透明度、节点颜色和流颜色
  geom_sankey(flow.alpha = .6,node.fill="#87CEEB",
              flow.fill="grey80",width=0.2) +
  # 添加桑基图的文本层，设置文本大小和颜色
  geom_sankey_text(size = 3,color="black") +
  # 手动设置填充颜色
  scale_fill_manual(values="grey80")+
  # 设置桑基图的主题，包括基础字体大小
  theme_sankey(base_size =15) +
  # 设置x轴标签为空，并将x轴放置在顶部
  labs(x = NULL) +
  scale_x_discrete(position = "top")+
  # 设置主题，包括隐藏图例和设置轴文本颜色
  theme(legend.position = "none",
        axis.text=element_text(color="black"))

```
### 绘制桑基图
>桑吉图节点的顺序可根据出现的频率通过定义因子的方式来调整顺序
```r
p2 <- map_data("world") %>% ggplot()+
  # 绘制世界地图的多边形，设置填充颜色为灰色
  geom_polygon(aes(x=long, y=lat, group=group), fill="grey")+
  # 在地图上添加点，表示df数据框中的位置数据
  geom_point(data=df, aes(longitude, latitude, fill=Continent, color=Continent),
             show.legend=F, size=1)+
  # 添加北箭头，位置在左下角，设置样式和颜色
  annotation_north_arrow(location="bl", pad_x=unit(0.03,"in"), pad_y=unit(3.5,"in"),
                         style=north_arrow_nautical(fill=c("grey40","white"),
                                                    line_col="grey20"))+
  # 在地图上添加一个矩形标注区域，设置位置、透明度和颜色
  annotate("rect", xmin=-128, xmax=-173, ymin=15, ymax=43, alpha=0.2, fill="blue")+
  # 在地图上添加文本标注，设置位置、大小、颜色和文本内容
  annotate("text", x=-150, y=30, hjust=0.5, size=3, color="black",
           label="North America\n5734 samples\n11 cover types\n10 habitats")+
  # 设置整个图表的主题，包括背景、面板背景、轴文本、轴刻度和轴标题
  theme(plot.background = element_blank(),
        panel.background = element_blank(),
        axis.text=element_blank(),
        axis.ticks = element_blank(),
        axis.title = element_blank())
```
### 拼图
```r
p1/p2+plot_layout(height = c(1.5,2))
```
![](https://files.mdnice.com/user/7338/8ee355be-5361-43d6-9fb4-a99825a580ad.png)

>本节介绍到此结束，有需要学习数据可视化的朋友，欢迎到小编的**淘宝店铺** **R语言数据分析指南**下单购买，内容主要包括各种**高分论文的图表分析复现以及一些个性化图表的绘制**均包含数据+代码；

