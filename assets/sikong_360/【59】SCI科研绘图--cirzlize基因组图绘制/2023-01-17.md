>本节来复现**nature communications**上的一张图`circos基因组图`由于作者为公开数据小编通过UCSC数据库检索构建了数据，结果仅供参考。**数据+代码已经上传2023年VIP群，加群的观众老爷请自行下载** 

### 论文原图
Proteogenomic characterization of MiT family translocation renal cell carcinoma

![](https://files.mdnice.com/user/7338/67e07357-a38a-4f8b-9504-8e58820e1406.png)



### 复现结果

![](https://files.mdnice.com/user/7338/a6bea01b-5fcd-4a21-9436-dd217bbe308c.png)


### 图形解读
>此图主要使用**circlize**包来进行绘制，绘图过程也是比较简单其主要还是对数据结构的理解以及一些细节的处理，下面来看小编的代码

### 加载R包
```
library(tidyverse)
library(circlize) 
```
### 清洗画布
```
circos.clear()
```
### 导入数据

此处对数据进行格式转换，使其符合绘图需求。

```r
bed <- read_tsv("bed.txt",col_names = F) %>% 
  separate(`X2`,into=c("chr","start"),sep=":") %>%
  separate(`start`,into=c("start","end"),sep="-") %>% 
  dplyr::rename(geneID="X1") %>% 
  mutate(start=as.numeric(start),end=as.numeric(end)) %>% select(2,3,4,1)
```
### 定义画布
```
circos.initializeWithIdeogram(plotType = NULL)
```


### 添加基因名称
connection_height调整基因线条的高度，side控制方向，labels.column = 4表示要展示的基因名称在数据的第四列。
```
circos.genomicLabels(bed,labels.column = 4,side = "outside",
                     connection_height=0.1,labels.side = side)
```
定义基因名称与染色体直接的间距
```
set_track_gap(mm_h(1))
```
### 绘制染色体轨道
若不需要边框可将bg.border="black"修改为bg.border="white"
```
circos.trackPlotRegion(ylim = c(0,0.1),track.height = 0.05,bg.border="black",panel.fun = function(x, y) {
  chr = CELL_META$sector.index
  xlim = CELL_META$xlim
  ylim = CELL_META$ylim
  circos.text(mean(xlim),mean(ylim),chr,cex = 0.5,
              col = "black",facing = "outside", niceFacing = TRUE)
  
})
```
### 绘制细胞条带
模式物种可以直接使用circlize包添加注释条带，默认为人的数据
```
circos.genomicIdeogram(track.height = mm_h(3)) # 条带高度为3
```
### 构建链接线数据
两份数据会对应链接因此根据共线性分析结果进行数据构建即可。
```
region1 <- read_tsv("data-1.txt",col_names = F) %>%
  separate(`X2`,into=c("chr","start"),sep=":") %>%
  separate(`start`,into=c("start","end"),sep="-") %>% select(-1) %>% 
  mutate(start=as.numeric(start),end=as.numeric(end))

region2 <- read_tsv("data-2.txt",col_names = F) %>% 
  separate(`X2`,into=c("chr","start"),sep=":") %>%
  separate(`start`,into=c("start","end"),sep="-") %>% select(-1) %>% 
  mutate(start=as.numeric(start),end=as.numeric(end))
```
### 添加链接线
根据需求添加颜色，比如在此我们将前6对定义为一组颜色，后4组为另一种颜色
```
circos.genomicLink(region1 %>% head(6),region2 %>% head(6),col="#3B9AB2")
circos.genomicLink(region1 %>% tail(4),region2 %>% tail(4),col="#F21A00")
```
